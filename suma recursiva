#include<iostream>
using namespace std;
int SUMADOR(int N);

int main() {
    int X,L,P;
    cout<<"hasta que numero sera la suma?"<<endl;
    cin>>X;
    P = SUMADOR(X);
    return 0;
}

int SUMADOR(int N){int k=0,p;
    for(int i = 0;i <= N;i++)
    {k =+ i;}
    cout<<k;
    return 0;
}
